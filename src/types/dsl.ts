/** Public DSL types, inferred from the Zod schema (single source of truth). */
export type {
  Netlist,
  NetlistMeta,
  Component,
  ComponentType,
  ComponentProps,
  Net,
  Terminal,
  TerminalRole,
  SwitchKind,
} from "@/lib/dsl/schema";
